# Custom command script
CMDSCRIPT /usr/local/bin/upssched-cmd

# Make sure upssched can communicate with with any running instances
PIPEFN /var/run/upssched.pipe
LOCKFN /var/run/upssched.lock

# When we lose mains power, start a timer
AT ONBATT * START-TIMER onbattwarn <%= graceseconds %>

# when we return to mains power, stop the timer:
AT ONLINE * CANCEL-TIMER onbattwarn
